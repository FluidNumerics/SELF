timeout: 1800s

steps:

- id: SELF Build
  name: 'gcr.io/cloud-builders/docker'
  args: ['build',
         '.',
         '-t',
         'gcr.io/${PROJECT_ID}/self:${_TAG}',
         ]

substitutions:
  _TAG: 'dev'

images: ['gcr.io/${PROJECT_ID}/self:${_TAG}']
