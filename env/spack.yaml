spack:
  view: ${HOME}/view/self
  config:
    install_tree: ${HOME}/software/self
  specs:
  - hipfort@4.5.2
  - hdf5@1.12.0+cxx+fortran+mpi
  - json-fortran@7.1.0
  - feq-parse@1.1.0
  - flap@master
  packages:
    cuda:
      buildable: false
      externals:
      - spec: cuda@11.2.0
        prefix: /usr/local/cuda
    hip:
      buildable: false
      externals:
      - spec: hip@4.5.2
        prefix: /opt/rocm/
