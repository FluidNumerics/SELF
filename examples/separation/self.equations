initial
{
u = 0.405/(1.0-0.35*( 1.0 - 0.7*tanh( (x-1.25)/0.25 ) ) )
w = -(0.405/( (1.0-0.35*( 1.0 - 0.7*tanh( (x-1.25)/0.25 ) ) )^2 ))*(1.0-z)*0.98*(sech( (x-1.25)/0.25 ) )^2
S = 0.5*(1.0-tanh(x/0.1))*(1.0+tanh((z-0.55)/0.01))*( sin( 25.132741224*(z-0.595)/0.405 ) )^2
}
boundary
{
h = 0.35*( 1.0 - 0.7*tanh( (x-0.25)/0.05 ) )
east = PRESCRIBED
west = PRESCRIBED
}
